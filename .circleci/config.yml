version: 2.1

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    docker:
      - image: alpine:latest
    resource_class: small
    steps:
      - run: env
      - setup_remote_docker
      - run: sleep 600
