version: 2.1

workflows:
  main:
    jobs:
      - test
      - build

jobs:
  test:
    machine:
      image: ubuntu-2004:202011-01
    resource_class: arm-large
    steps:
      - checkout:
          path: project
  build:
    circleci_ip_ranges: true
    docker:
      - image: cimg/go:1.18
    steps:
      - checkout:
          path: project  